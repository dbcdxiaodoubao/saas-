<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mashang.ordering.mapper.MsMenuMapper">

    <!-- 查父菜单 -->
    <select id="getParentMenuByMealId" resultType="com.mashang.ordering.domain.vo.MsMenuListVo">
        select t1.menu_id,
               t1.menu_name
        from sys_menu t1
        left join ms_meal_menu t2
            on t1.menu_id = t2.menu_id
        where t2.meal_id = #{mealId}
          and t1.parent_id = 0
    </select>

    <!-- 查子菜单 -->
    <select id="getChildMenuByParentId" resultType="com.mashang.ordering.domain.vo.MsMenuChildListVo">
        select menu_id, menu_name
        from sys_menu
        where parent_id = #{parentId}
    </select>
</mapper>